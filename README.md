# live-title
The title for live stream

## Installation

Install pip first
``` bash
sudo easy_install pip
```

Install package
``` bash
sudo pip install Flask
sudo pip install pymongo
```

## Usage

run
``` bash
python backend.py
```